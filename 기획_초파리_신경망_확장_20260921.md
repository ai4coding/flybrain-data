# 기획서 — 초파리 신경망 실험실 확장 (`flybrain`)

> 작성 2026-09-21 · **코드 수정 0** · 상태: 기획(사용자 결정 대기 항목은 §12)
> 목적: 초파리 **연결지도(커넥톰) 기반 신경망 모델**을 브릭셀AI 블록으로 실행해, 같은 "두뇌"로
> ① 무대 위 가상 초파리(길찾기) ② 학생이 만든 게임 캐릭터 ③ 실제 로봇 자동차를 움직이고,
> 학생이 **뉴런 집단을 억제·변경하며 행동 변화를 실험**하게 한다. (생명과학 × AI × 로봇)
> ⚠️ 이 문서는 지도다. 뉴런 ID·파라미터·파일 크기·포트·API 는 **착수 시 원본 저장소와 코드에서 재확인**.
> ⚠️ 구현 에이전트는 작업 전 루트 `CLAUDE.md` 를 먼저 읽는다(백업 규칙·l10n 빌드·브라우저 확인 규칙·배포 금지 규칙).
> ⚠️ 저장 위치 메모: 이 파일은 `G:\내 드라이브\...\mono-repo_ai4coding.github.io_V2\dev-logs\` 에 있다.
> 루트 `CLAUDE.md` 는 메인 폴더를 `D:\00_5project_Final\...` 로 적고 있다 — **어느 쪽이 작업 정본인지 착수 전 사용자에게 확인.**

---

## 0. 한 줄 결론

- **가능하다. 단 "뇌 파일을 불러오면 미로를 푼다"가 아니다.** 만들 것은 ①신경망 실행기 ②감각 입력 변환기 ③행동 출력 변환기 3개이고,
  ②③은 **사람이 설계한 매핑**이다. UI·가이드에 그렇게 표기한다(§10 표기 규정).
- **전뇌를 매번 돌릴 필요 없다.** 기본 엔진 = 전뇌에서 잘라낸 **부분 회로(수천 뉴런) LIF 모델을 브라우저 Web Worker 에서** 실행.
  서버 0 · 설치 0 · GitHub Pages 정적 배포 유지. 전뇌 실시간은 **선택 설치 로컬 앱**(4단계)으로만.
- 근거(2026-09-21 웹 확인): 전뇌 138,639 뉴런·연결 ~1,500만의 LIF 를 PyTorch+Triton+CUDA graph 로 최적화해도
  **RTX 4080 SUPER 에서 실시간 1.2배**(fly-survivors README). 교실 크롬북·내장그래픽에서 전뇌 실시간은 불가로 본다.
- 오가는 데이터는 사소하다(입력 수십 개·출력 2개·20Hz ≈ 수 KB/s). **병목은 통신이 아니라 계산·지연** → 클라우드 서버 안은 기각(§3-1).
- **블록은 하나, 엔진은 교체 가능**: 확장은 `FlyEngine` 인터페이스(§3-2)만 본다. Worker 엔진과 로컬 앱 엔진이 **같은 메시지 규격**을 쓴다.
- 원본 LIF 모델(Shiu 2024)에는 **학습(가소성)이 없다.** → `보상 주기`·`학습 시작` 류 블록은 **만들지 않는다**(§5-6).
- 최대 불확실성 = **"어떤 감각 입력 집단이 좌/우 조향 출력(DNa02 L/R)을 비대칭으로 움직이는가"**. 이건 문헌이 아니라
  **0단계 스파이크 실측**으로만 정해진다. 안 나오면 대안 경로(§9-0 게이트)로 간다.

---

## 1. 기존 자산 (코드 실측 2026-09-21) — 무엇을 물려받고 무엇을 안 하나

| 자산 | 위치 | 이번에 쓰는 것 | 안 따라가는 것 |
|---|---|---|---|
| 강화학습 `rlmachine` | `scratch-vm/src/extensions/scratch3_rlmachine/` (l10n prefix `rl.`) | "센서 → 상태 → 행동" 블록 어휘, 모델 저장/불러오기 UX, Sim2Real 예제 구도. **비교 실험의 대조군**(같은 센서로 Q러닝 vs 초파리 회로) | Q테이블·카메라 라인센서 로직 |
| 모방학습 `behaviorcloning` | `scratch3_behaviorcloning/` | 대조군 2 (시연 학습 제어기) | |
| 물리 `physics` | `scratch3_physics/` (Matter.js) | 게임 예제에서 충돌·물체 운동 담당. flybrain 은 **행동 결정만** | 물리 재구현 금지 |
| 비전 워커 | `shared/vision-worker-entry.js` · `vision-worker-client.js` + `scratch-gui/webpack.config.js`(vm 빌드 워커 번들 복사) | **워커 번들 배선 선례**(vm 엔트리 3곳 + gui 복사 **둘 다** 필요. 한쪽만 하면 dev 는 되고 배포본 404 — webpack 주석의 함정 ①②) | MediaPipe 관련 전부 |
| 성능 계측 | `shared/perf-monitor.js` (`brixelPerf`) | step 1회 벽시계 시간·긴 프레임 계측 | |
| GPU 감지 | `shared/gpu-capability.js` | 3단계(WebGPU 전뇌)에서만. **"GPU=빠름"은 워크로드 의존**(루트 CLAUDE.md 0-U 실측) — 실측 대결 없이 채택 금지 | 1~2단계에서 GPU 사용 |
| 자동차 확장 | `scratch3_unocar` · `espcar` · `mabitcar` (keep-alive·워치독·`PROJECT_STOP_ALL`·자동 재연결 구현됨, 펌웨어 v1.3/v0.5/v1.6) | **차 제어는 기존 블록을 그대로 조합**한다. flybrain 은 `전진/회전 출력` 리포터만 제공 | flybrain 안에 시리얼·BLE 코드 넣기 **금지** |
| 로컬 에이전트 선례 | esp32cam `127.0.0.1:8765` · phonecam `8766/9443` · ailocal `9200` · arm5axis `8080/8081` · TTS `9000` · 교실서버 `17443` | 4단계 로컬 앱의 접속 패턴(`ws://127.0.0.1`). 새 포트는 위와 겹치지 않게(제안 **9300**, 착수 시 재확인) | |
| 정적 모델 파일 선례 | `scratch-gui/static/yolov8n_q.onnx`(3.5MB) 등 | 경량 모델 파일을 `scratch-gui/static/flybrain/` 에 둔다(목표 ≤ 5MB) | 전뇌 파일(추정 75~120MB)을 GH Pages 에 두기 — 파일당 100MB 제한·대역폭 병목 |
| 예제·도구 | `examples_sb3/_tools/` · `packages/_tools_agent_catalog/` | 예제 sb3 생성, 도우미 카탈로그 5파일 갱신 | |

**확인된 부재**: `scratch3_*fly*` · `*neuro*` · `*brain*` · `*connectome*` 폴더 없음(확장 106개 중, 2026-09-21 `ls` 실측). `brixelai` 는 AI 도우미라 무관.

**제약(재확인됨)**: 에디터가 https 라 `ws://<LAN IP>` 는 mixed content 로 차단(루트 CLAUDE.md 0-V 기각 목록). 예외는 루프백 `ws://127.0.0.1` 뿐
→ "교사 PC 1대가 학급 전체 뇌를 계산" 구도는 **기본 경로로 불가**. 하려면 로컬 앱이 에디터 자체를 http 로 서빙해야 한다(범위 외, §12-6).

---

## 2. 사용 시나리오 (학생 시점)

### 2-1. 가상 초파리가 먹이를 찾아간다 (1단계)
1. 확장 추가 → `초파리 신경망 [주행 회로] 불러오기` (첫 1회 수 MB 다운로드, 진행 표시).
2. 무한 반복: 왼쪽/오른쪽 더듬이 위치에서 먹이까지 냄새 세기 계산 → `감각 입력 [왼쪽 냄새] 에 (값) 넣기` →
   `신경망을 [50] ms 계산하기` → `(회전 출력)` 만큼 돌고 `(전진 출력)` 만큼 이동.
3. `뇌 활동 창 보이기` 로 어떤 뉴런 집단이 켜지는지 본다(2단계).

### 2-2. 회로를 망가뜨려 본다 (2단계 — 이 확장의 핵심 가치)
4. `뉴런 집단 [오른쪽 조향 뉴런] 억제하기` → 초파리가 한쪽으로만 돈다 → 왜? 를 토론.
5. 같은 미로·같은 센서로 **규칙 기반 / Q러닝(`rlmachine`) / 초파리 회로** 3종의 성공률·충돌·시간을 표로 비교.

### 2-3. 같은 두뇌로 자동차를 움직인다 (2단계)
6. 가상 센서 자리에 우노카 초음파·라인센서 값을 넣고, 출력 리포터를 우노카 모터 블록에 연결. **flybrain 블록은 그대로.**

---

## 3. 아키텍처

```
[감각원]  무대 스프라이트 계산값 / 가상 더듬이 / 자동차 센서(unocar·espcar 블록)
    │  0~100 값
    ▼
[입력 변환기]  값 → 입력 뉴런 집단의 포아송 발화율 (0 ~ maxRateHz)          ← 사람이 설계한 매핑
    ▼
[FlyEngine]  LIF 시뮬레이션 (상태 유지: 막전위 v, 시냅스 g, 불응기, 지연 큐)
    │    ├─ LiteWorkerEngine   : 부분 회로, Web Worker (기본, 1단계)
    │    ├─ WebGpuEngine       : 전뇌, 실험적 (3단계)
    │    └─ AgentEngine        : ws://127.0.0.1:9300 로컬 앱, 전뇌 (4단계)
    ▼
[출력 변환기]  하행 뉴런(DN) 집단 발화율 → 전진 −100~100 · 회전 −100~100    ← 사람이 설계한 매핑
    ▼
[몸체]  무대 스프라이트 / 게임 캐릭터(physics) / 자동차(기존 하드웨어 블록)
    └──────────── 움직임이 다음 감각을 바꾼다 (폐루프) ────────────┘
```

- **시뮬레이션 시간 ≠ 벽시계 시간.** `신경망을 [50] ms 계산하기` 는 시뮬 시간 50ms 를 전진시키고 Promise 로 끝난다.
  가상환경은 시뮬 시간에 맞춰 흐르므로 **계산이 느려도 결과는 같고 화면만 느려진다.** 실시간이 필요한 건 실제 자동차뿐 → 자동차엔 경량 엔진.
- 재현성: 포아송 입력은 **시드 고정 PRNG**(예: mulberry32). 같은 시드·같은 입력열 → 같은 출력. 수업에서 "왜 내 건 달라요?"를 막고 단위시험을 가능케 한다.

### 3-1. 실행 위치 비교

| 안 | 설치 | 비용·확장성 | 지연 | 판정 |
|---|---|---|---|---|
| **A. 브라우저 Worker + 부분 회로** | 0 | 0원, 학생 PC 가 각자 계산 | 0 | ★**채택(기본)** |
| B. 브라우저 WebGPU + 전뇌 | 0 (+대용량 다운로드) | 0원, 데스크톱 GPU 한정 | 0 | 3단계 실험 기능. 실측 대결 후 |
| C. 로컬 앱(Windows, `ws://127.0.0.1`) + 전뇌 | **필수** — 학교 PC 설치 권한·크롬북 불가 | 0원 | ~1ms | 4단계 **선택** 모드 |
| D. 클라우드 서버 | 0 | **학생 1명 = 상시 시뮬 1개**. 30명 = 고성능 GPU 수십 장 분량 | 50~200ms + 학교망 | **기각** |
| E. 사전계산 표/증류 소형망 | 0 | 0원, ESP32 온보드 가능 | 0 | 5단계. 뉴런 억제 실험은 사전 계산한 조합만 가능 |

### 3-2. `FlyEngine` 인터페이스 (확장은 이것만 호출. Worker·에이전트 **동일 메시지 규격**)

```
요청 → 응답 (JSON, id 로 짝 맞춤. Worker 는 postMessage, 에이전트는 WebSocket 텍스트)
{op:'load',   model:'drive-lite-v1', brain:1}            → {ok, meta:{n, nnz, groups, inputs, outputs, dtMs}}
{op:'reset',  brain:1, seed:12345}                       → {ok}
{op:'input',  brain:1, name:'odorL', value:0..100}       → (응답 없음, 다음 step 에 반영)
{op:'step',   brain:1, ms:50}                            → {simTime, forward, turn, rates:{그룹명:Hz}, escape:bool, wallMs}
{op:'silence',brain:1, group:'DNa02_R', on:true}         → {ok}
{op:'scale',  brain:1, from:'grpA', to:'grpB', pct:50}   → {ok}            (2단계)
{op:'raster', brain:1, group:'…', lastMs:500}            → {spikes:[[t,i]…]} (2단계, 시각화용)
{op:'dispose',brain:1}
```
- `brain` 슬롯(1~4)을 **처음부터 규격에 넣는다**(1단계 블록은 1 고정). 게임에서 초파리 여러 마리를 쓰려 할 때 규격을 깨지 않기 위함.
- `step` 은 직렬화: 앞 step 이 안 끝났는데 또 오면 **큐에 1개만 두고 나머지는 같은 Promise 를 공유**(무한반복 + 복제본 폭주 방지).

---

## 4. 원본 모델·데이터 (★착수 시 전부 재확인)

| 항목 | 값(기억·웹 확인 기준) | 확인처 |
|---|---|---|
| 원본 모델 | Shiu et al., *A Drosophila computational brain model reveals sensorimotor processing*, Nature 2024. 저장소 `philshiu/Drosophila_brain_model` — `model.py` · `utils.py` · `example.ipynb` · `Connectivity_783.parquet` · `Completeness_783.csv` (630 판 파일도 공존 — **버전 섞지 말 것**) | GitHub |
| 시뮬레이터 | Brian2 (C++ codegen 권장). "raw output 수 GB" 명시 | README |
| 규모 | 뉴런 138,639 · 연결(뉴런쌍) ~1,500만 · 시냅스 5천만+ | fly-survivors README / 논문 |
| LIF 식 | `dv/dt = (v_0 − v + g)/t_mbr` · `dg/dt = −g/tau` · 스파이크 시 `v→v_rst`, 불응기 `t_rfc`, 시냅스 지연 `t_dly` 뒤 `g += (시냅스 수 × 부호) × w_syn` | `model.py` |
| 파라미터(기억값 — **model.py 에서 재확인 필수**) | `v_0=v_rst=−52mV` · `v_th=−45mV` · `t_mbr=20ms` · `tau=5ms` · `t_rfc=2.2ms` · `t_dly=1.8ms` · `w_syn=0.275mV` · 입력 포아송 `r_poi=150Hz`·`f_poi=250` · `dt=0.1ms` | `model.py` `default_params` |
| 부호 | 신경전달물질 예측으로 흥분(+)/억제(−). 연결 파일의 가중치 열이 이미 부호 포함인지 확인 | parquet 열 이름 |
| 입력·출력 조작 | 자극 = 지정 뉴런에 포아송 발화 / 억제 = 그 뉴런의 입·출 시냅스 0 / 출력 = 전 뉴런 스파이크 시각·발화율. 뉴런은 FlyWire root ID 로 지정 | README |
| 세포 유형 주석 | `flyconnectome/flywire_annotations` (Supplemental neuron annotations TSV — cell_type·side·super_class) 또는 Codex(codex.flywire.ai) | GitHub |
| 출력 후보(하행 뉴런) | `DNa02` L/R = 동측 회전 · `DNp09` = 전진 보행 · `MDN` = 후진 · `DNp01`(거대섬유) = 탈출 — fly-survivors 가 같은 집단을 50ms 창 평균 발화율로 읽는다 | fly-survivors README + 문헌 |
| 논문이 검증한 회로 | 당(sugar) GRN → MN9(주둥이 뻗기), 쓴맛에 의한 억제, 존스턴기관 → 더듬이 그루밍 회로 | 논문 |
| **알려진 한계** | 이 LIF 는 **시각 운동 계산을 못 한다**(T4/T5 미발화) · 광수용체 시냅스 부호 문제 → fly-survivors 는 medulla OFF 세포(Tm1/2/4/9)에 **직접 주입**으로 우회. 학습·가소성·신경조절·간극결합 없음 | fly-survivors README |
| **라이선스** | Shiu 저장소 라이선스 · FlyWire 데이터 이용 조건(인용 의무·**상업적 이용 가능 여부**) **미확인** → §12-1 | 각 저장소 LICENSE · flywire.ai |

참고 구현(읽기만, 코드 복사는 라이선스 확인 뒤): `eonsystemspbc/fly-brain`(Brian2·Brian2CUDA·PyTorch·NEST GPU 벤치마크) ·
`arisson2001rojas-design/fly-survivors`(전뇌 LIF 로 게임 조작) · `erojasoficial-byte/fly-brain`(NeuroMechFly 몸체 연결).
사용자 제공 요약에 나온 FlyGM 논문(전뇌 그래프 모델 + 모방·강화학습, 코드 "Coming soon")·DOOMFLY 는 **URL 미확인** — 인용 전 확인.

---

## 5. 확장 블록 정의 (`flybrain`)

**이름 규칙**: 폴더 `scratch3_flybrain` · id `flybrain` · l10n prefix `flybrain.`(id 와 동일하게 — rlmachine 의 `rl.` 같은 불일치를 만들지 말 것) ·
클래스 `Scratch3FlyBrain` · 카드명 `초파리 신경망 실험실` · GUI 카드는 `index.jsx` 의 `rlmachine` 카드 **바로 다음** ·
`internetConnectionRequired: true`(모델 파일 다운로드) · 아이콘 `extensions/flybrain/flybrain.webp`(600×372) + `flybrain-small.webp`(180×180), **원본 png 보존·고유 iconURL**.

### 5-1. 준비 (5)

| opcode | 블록 | 유형 | 단계 |
|---|---|---|---|
| `loadModel` | `초파리 신경망 [MODEL] 불러오기` MODEL=`주행 회로(경량)` … | COMMAND(Promise, 진행 표시) | 1 |
| `isReady` | `<신경망 준비되었는가?>` | BOOLEAN | 1 |
| `resetState` | `신경망 상태 초기화하기` (막전위·입력·억제 전부 초기, 시뮬 시간 0) | COMMAND | 1 |
| `setSeed` | `무작위 씨앗을 [N] 으로 정하기` | COMMAND | 1 |
| `getInfo2` | `신경망의 [뉴런 수/연결 수/시뮬레이션 시간(ms)/계산 속도(배)]` | REPORTER | 1 |

### 5-2. 감각 입력 (3)

| opcode | 블록 | 유형 | 단계 |
|---|---|---|---|
| `setInput` | `감각 입력 [INPUT] 에 [VALUE] 넣기` VALUE 0~100(범위 밖은 자름). INPUT 메뉴는 **모델 meta 의 `inputs` 에서 동적 생성** | COMMAND | 1 |
| `clearInputs` | `감각 입력 모두 0 으로` | COMMAND | 1 |
| `getInput` | `감각 입력 [INPUT] 의 현재 값` | REPORTER | 1 |

### 5-3. 계산·출력 (6)

| opcode | 블록 | 유형 | 단계 |
|---|---|---|---|
| `stepMs` | `신경망을 [MS] ms 계산하기` 기본 50, 허용 1~1000 | COMMAND(Promise) | 1 |
| `getForward` | `(전진 출력)` −100~100 | REPORTER | 1 |
| `getTurn` | `(회전 출력)` −100~100, **오른쪽이 +**(스크래치 `오른쪽으로 돌기` 와 같은 부호) | REPORTER | 1 |
| `getWheel` | `([왼쪽/오른쪽] 바퀴 출력)` = clamp(전진 ± 회전) — 차동 구동 차량용 편의 | REPORTER | 2 |
| `getOutputRate` | `출력 뉴런 [OUTPUT] 의 발화율(Hz)` | REPORTER | 1 |
| `whenEscape` | `탈출 신호가 발화했을 때` (거대섬유 집단, `startHats`, `isEdgeActivated:false`) | HAT | 2 |

### 5-4. 실험 (7)

| opcode | 블록 | 유형 | 단계 |
|---|---|---|---|
| `silenceGroup` | `뉴런 집단 [GROUP] 억제하기` | COMMAND | 1 |
| `restoreGroup` | `뉴런 집단 [GROUP] 되살리기` (메뉴에 `모두` 포함) | COMMAND | 1 |
| `isSilenced` | `<뉴런 집단 [GROUP] 억제되었는가?>` | BOOLEAN | 1 |
| `getGroupRate` | `뉴런 집단 [GROUP] 의 활동(Hz)` | REPORTER | 1 |
| `scaleWeights` | `[FROM] → [TO] 연결 세기를 [PCT] % 로` | COMMAND | 2 |
| `showActivity` | `뇌 활동 창 [보이기/숨기기]` — 무대 오버레이: 집단별 막대 + 최근 500ms 래스터. 문구 전부 `formatMessage` | COMMAND | 2 |
| `logRow` | `실험 기록에 [LABEL] 줄 추가` → 시뮬시간·입력·출력·억제목록을 내부 표에 적재, `실험 기록 CSV 로 저장` 과 짝 | COMMAND ×2 | 2 |

### 5-5. 가상 감각 도우미 (3, 2단계 — 없어도 1단계 예제는 스크래치 기본 블록으로 가능)

| opcode | 블록 | 유형 |
|---|---|---|
| `odorAt` | `[왼쪽/오른쪽] 더듬이에서 [SPRITE] 의 냄새 세기` = 100·exp(−d/λ). 더듬이 점 = 스프라이트 방향 ±30°, 20px 앞 | REPORTER |
| `setOdorRange` | `냄새가 퍼지는 거리를 [PX] 로` (λ) | COMMAND |
| `wallAhead` | `[왼쪽/오른쪽/앞] 더듬이 [DIST] 걸음 안의 벽 정도(색 [COLOR])` 0~100 — 구현 방식은 착수 시 scratch-render API 확인(후보: 선분 위 N점 색 샘플링). **비용 계측 필수** | REPORTER |

### 5-6. 엔진 (2, 4단계) · 만들지 않는 블록

| opcode | 블록 | 단계 |
|---|---|---|
| `setEngine` | `계산 엔진을 [자동/브라우저(경량)/로컬 앱(전뇌)] 로` | 4 |
| `getEngine` | `(현재 계산 엔진)` — **번역된 표시값** | 4 |

⛔ **만들지 않는다**: `보상 주기` · `학습 시작/멈춤` · `기억하기` — 원본 모델에 가소성이 없다. 학습 규칙을 **실제로 구현·검증**한 모델이 생기기 전엔 금지.
"신경망이 계속 돈다 = 학습한다"는 오개념을 블록 이름이 심으면 안 된다.

**합계 26 (1단계 15 · 2단계 9 · 4단계 2).** 메뉴: MODEL · INPUT · OUTPUT · GROUP(전부 meta 에서 동적) · SIDE · INFO.
메뉴 표시명은 meta 의 `label.{ko,en,…}` — 없는 로케일은 en 폴백. 저장된 sb3 에는 **그룹의 영문 키**가 남게 한다(번역값 저장 금지).

### 5-7. 동작 규약 (구현 시 그대로)

- **생성자는 상태만.** 확장을 팔레트에 추가하는 것만으로 워커 생성·모델 다운로드를 시작하지 않는다 — `loadModel` 에서만.
- 모델 미로드 상태: 리포터 0 · 불리언 false · hat 미발동 · `stepMs` 는 즉시 반환(에러 던지지 않음) + 1회 안내(`shared/ml-notify.js` 패턴 확인).
- **빨간 정지(`PROJECT_STOP_ALL`)**: 입력 전부 0 · 전진/회전 출력 0 · 진행 중 step 결과 폐기. **모델·억제 설정은 유지**(개발 중 정지를 수십 번 누른다). 자동차가 출력 리포터를 읽고 있어도 0 이 되어 멈춘다(차 자체 안전장치는 기존 확장 몫).
- 출력 계산: 최근 `windowMs`(기본 50) 창의 집단 평균 발화율 → `forward = gainF × (DNp09 평균 − kB × MDN 평균)`, `turn = gainT × (DNa02_R − DNa02_L)` → clamp −100~100. 이득·창은 **meta 에 저장된 파이프라인 보정값**(§6 파이프라인 4번). 코드에 하드코딩 금지.
- 입력 변환: `rateHz = value/100 × maxRateHz`(meta). 집단의 각 뉴런에 독립 포아송(dt 당 확률 `rate×dt`), 스파이크 시 `v += w_in` 방식은 **원본 `model.py` 의 포아송 입력 구현과 동일하게**(f_poi 배율 포함) 맞춘다.
- 억제: 해당 뉴런의 **나가는·들어오는 시냅스 효과 0**(원본과 같은 정의). 구현은 CSR 을 고치지 말고 `silenced[i]` 마스크로(되살리기가 O(1)).

---

## 6. 오프라인 파이프라인 (Python, 에디터 빌드와 분리)

위치 제안: `scratch-editor/packages/_tools_flybrain/`(선례 `_tools_agent_catalog`). 대용량 원본(parquet)·중간 산출물은 **`.gitignore`**(루트 CLAUDE.md "`.gitignore` 완화 금지" 절 확인 후 항목 추가). README 에 conda 환경·실행 순서.

1. **`00_reproduce.py`** — 원본 Brian2 모델을 783 파일로 실행. 논문 검증 회로 재현: 당 GRN 자극 → MN9 발화 / 쓴맛 동시 자극 시 감소.
   합격 = 방향성 재현(수치는 `example.ipynb` 결과와 대조). **이게 안 되면 이후 전부 무의미 — 여기서 멈추고 보고.**
2. **`01_screen_io.py`** — ★핵심 스크리닝. 주석 TSV 에서 감각 집단 후보(후각 ORN 유형별·미각 GRN·존스턴기관·상승 기계감각·LC/LPLC 시각투사 등)를
   **좌/우 따로** 100~200Hz 자극 → DNa02 L/R · DNp09 · MDN · DNp01 발화율 기록. 산출 = `screen.csv`(집단×출력 행렬) + **좌우 비대칭 지수**.
3. **`02_extract.py`** — 채택한 입력 집합 I·출력 집합 O 에 대해 부분 회로 추출. 방식 = **활동 기반**(전뇌 실행에서 기준 대비 발화율 변화 > θ 인 뉴런 ∪ I ∪ O)
   + **경로 기반**(I→O 최단 k홉 이내, 시냅스 수 문턱) 의 합집합. 목표 N ≤ 10,000(넘으면 θ·k 조정).
4. **`03_validate.py`** — 축소 회로 vs 전뇌를 **입력 격자(각 입력 0/25/50/75/100 조합 표본 ≥ 200점, 시드 5개)**에서 비교.
   합격선(제안): 출력 집단 발화율 Pearson r ≥ 0.9 **그리고** 전진/회전 출력 부호 일치율 ≥ 95%. 미달이면 3으로 돌아가 N 을 늘린다.
   같은 스크립트가 출력 이득 `gainF/gainT/kB` 를 보정(입력 격자에서 출력이 −100~100 을 고르게 쓰도록)해 meta 에 기록.
5. **`04_export.py`** — 웹용 포맷(§6-1) + **JS 엔진 대조용 픽스처**(§9-1) 생성.
6. **`05_table.py`**(5단계) — 입력 격자 → 출력 표/소형 MLP 증류. 전뇌 vs 표의 폐루프 궤적 차이 보고(재귀 회로의 이력 의존성 확인).

### 6-1. 모델 파일 포맷 v1 (`scratch-gui/static/flybrain/`)

- `drive-lite-v1.json` (meta):
```json
{
  "format": 1, "id": "drive-lite-v1",
  "source": {"connectome": "FlyWire v783", "model": "Shiu et al. 2024 LIF", "pipelineCommit": "…", "builtAt": "…"},
  "n": 0, "nnz": 0, "dtMs": 0.1,
  "params": {"vRest": -52, "vReset": -52, "vTh": -45, "tauM": 20, "tauSyn": 5, "tRef": 2.2, "tDelay": 1.8, "wSyn": 0.275},
  "groups":  {"DNa02_L": {"role": "output", "side": "L", "indices": [..], "label": {"ko": "왼쪽 조향 뉴런", "en": "Left steering DN"}, "flywireIds": ["…"]}},
  "inputs":  {"odorL": {"group": "ORN_x_L", "maxRateHz": 150, "label": {..}, "designNote": "사람이 설계한 매핑: …"}},
  "decode":  {"windowMs": 50, "forward": {"plus": ["DNp09_L","DNp09_R"], "minus": ["MDN"], "gain": 0, "kB": 0}, "turn": {"plus": ["DNa02_R"], "minus": ["DNa02_L"], "gain": 0}, "escape": {"group": "DNp01", "minHz": 0}},
  "validation": {"pearsonR": 0, "signAgreement": 0, "gridPoints": 0, "seeds": 5},
  "bin": {"file": "drive-lite-v1.bin", "sha256": "…", "layout": [{"name": "rowptr", "dtype": "int32", "length": 0}, {"name": "col", "dtype": "int32", "length": 0}, {"name": "w", "dtype": "int16", "length": 0}]},
  "viz": {"xy": "선택 — 집단별 2D 좌표(시각화용)"}
}
```
- `drive-lite-v1.bin`: CSR(시냅스 **전**뉴런 행 → 후뉴런 열, 스파이크 난 행만 훑는 이벤트 구동용). `w` = 부호 있는 시냅스 수(int16 넘으면 clamp 하고 로그).
- 파일명에 버전. 내용이 바뀌면 **v2 새 파일**(CDN·브라우저 캐시가 옛 파일을 물고 있어도 안 깨지게). sha256 검증 실패 시 로드 거부.

---

## 7. JS 엔진 (`LiteWorkerEngine`)

- 파일 배치(제안): `scratch3_flybrain/index.js`(블록·메뉴·정지훅) · `engine-client.js`(FlyEngine 구현, Promise/id 매칭, 직렬화) ·
  `lif-core.js`(**DOM·워커 의존 0 의 순수 모듈** — node 단위시험이 직접 import) · `flybrain-worker-entry.js`(lif-core 를 감싼 워커).
- **워커 번들 배선은 vision-worker 선례를 그대로 따른다**(vm webpack 엔트리 + gui `CopyWebpackPlugin` 복사 **둘 다**, dev 서버는 vm 빌드 후 재시작 필요).
  Blob+`Function.toString()` 인라인 워커는 babel 헬퍼·minify 로 깨질 수 있어 **비채택**. 착수 시 `shared/CLAUDE.md` 와 webpack 주석(함정 ①②) 정독.
  `_headers` 의 CSP `worker-src` 도 확인.
- 자료구조: `Float64Array v,g` (원본이 double — 픽스처 일치 우선. 성능 모자라면 Float32 로 내리고 **재검증**) · `Uint16Array refLeft` · `Uint8Array silenced` ·
  지연 링버퍼 `delaySteps = round(tDelay/dt)` 칸, 각 칸 = 그 스텝에 도착할 `Float64Array dg` 누적 또는 스파이크 목록.
- 1스텝: ①도착분 `g += dg` ②포아송 입력 ③`v,g` 적분(원본과 같은 방법 — Brian2 `method` 가 exact/linear 인지 `model.py` 확인 후 **같은 닫힌 해** 사용)
  ④문턱 넘은 뉴런 스파이크·리셋·불응 ⑤스파이크 난 행의 CSR 을 훑어 `delaySteps` 뒤 칸에 가중치 적재(억제 마스크 반영) ⑥출력 집단 스파이크 카운트.
- 예산(목표, `brixelPerf` 로 실측): `step(50ms)` = 500스텝. N=5,000 기준 데스크톱 ≤ 15ms · **크롬북 ≤ 40ms**. 초과 시 N 축소 → 그래도 안 되면 dt 0.2ms(재검증 필수).
- 워커 1개가 brain 슬롯 1~4 를 모두 담당(모델 버퍼 공유, 상태만 슬롯별).

---

## 8. 예제 (교사 가이드에 그대로 실을 것)

### 8-1. 먹이 찾는 초파리 — 1단계 블록만
```
[깃발 클릭]
  초파리 신경망 [주행 회로(경량)] 불러오기
  무작위 씨앗을 [1] 로 정하기 · 신경망 상태 초기화하기
  무한 반복
    감각 입력 [왼쪽 냄새] 에 (왼쪽 더듬이→먹이 거리로 계산한 값) 넣기
    감각 입력 [오른쪽 냄새] 에 (오른쪽 …) 넣기
    신경망을 [50] ms 계산하기
    (회전 출력 × 0.15) 도 돌기 · (전진 출력 × 0.05) 만큼 움직이기
  끝
```
배우는 것: 감각→신경→운동 폐루프, 좌우 비대칭이 방향을 만든다(브라이텐베르크 차량과 비교).

### 8-2. 회로 절단 실험 — 2단계
`스페이스 키` → `뉴런 집단 [오른쪽 조향 뉴런] 억제하기` → 궤적을 펜으로 그려 억제 전·후 비교 → `실험 기록 CSV 로 저장` → 차트 확장으로 그래프.

### 8-3. 세 두뇌 대결 — 2단계
같은 미로·같은 가상 더듬이 값으로 ①규칙 기반(만약 왼쪽 벽이면 오른쪽) ②`rlmachine` Q러닝 ③flybrain. 20회씩 성공률·충돌·시간 표.
**초파리 회로가 질 수도 있다 — 그 결과를 그대로 싣는다**(§10).

### 8-4. 같은 두뇌, 진짜 자동차 — 2단계
`감각 입력 [왼쪽 장애물]` ← 우노카 센서 블록 값 환산, `우노카 모터` ← `(왼쪽/오른쪽 바퀴 출력)`. 저속 제한은 학생 스크립트가 아니라 **기존 우노카 확장·펌웨어 안전장치**가 최종 방어선.

---

## 9. 단계·게이트·검증

### 9-0. ★0단계 게이트 (Go / 우회 / 축소)
| 스크리닝 결과 | 진행 |
|---|---|
| 자연 감각 집단(예: 좌/우 ORN) 자극이 DNa02 L/R 을 **비대칭**으로 움직임 | 그대로 1단계. 입력 = 그 감각 집단 |
| 감각 말단에선 안 나오지만 **중간 집단**(예: 시각 투사 LC/LPLC, 조향 상류 집단)에 주입하면 나옴 | 1단계 진행하되 입력 이름·가이드에 **"감각기관이 아니라 뇌 중간 단계에 직접 넣는다"** 명시(fly-survivors 와 같은 우회) |
| 어느 쪽도 조향 비대칭이 안 나옴 | **주행 회로 보류.** 논문이 검증한 회로(당/쓴맛 → 먹이 반응, 더듬이 → 그루밍)로 "자극·억제 실험실" 을 1단계로 축소하고 사용자에게 보고 |

### 9-1. 검증 계층
1. **원본 재현**(§6 파이프라인 1번) — Brian2 결과가 논문/노트북과 같은 방향.
2. **축소 회로 vs 전뇌**(§6 파이프라인 4번) — 합격선 수치.
3. **JS 엔진 vs Python 기준** — 픽스처: 같은 축소 회로 + **미리 생성해 둔 입력 스파이크 시각열**(포아송 RNG 차이 제거) 1초분 → 출력 집단 스파이크 수 일치(허용 ±1~2%).
   `scratch-vm/test/unit/flybrain_lif_core.js` 로 node 에서 실행. 억제·리셋·시드 재현성 시험 포함.
4. **화면 E2E** — 에이전트가 앱 내장 브라우저로 `localhost:8601` 에서 8-1 예제 실행·콘솔 에러 0·`brixelPerf` 수치 기록(루트 CLAUDE.md 9번 브라우저 규칙).
5. **행동 벤치마크** — 8-3 의 3종 비교를 시드 고정 20회. 결과 표를 dev-logs 에.
6. **실기**(자동차·크롬북) — 사용자.

### 9-2. 단계표
| 단계 | 내용 | 산출물 | 예상 |
|---|---|---|---|
| **0 스파이크** | 파이프라인 1~2번(재현 + I/O 스크리닝) · 라이선스 확인 · 게이트 판정 | `dev-logs/실측_flybrain_스파이크_YYYYMMDD.md` · `screen.csv` | 2~3일 |
| **1 MVP** | 파이프라인 3~5번 · `lif-core` + 워커 + 1단계 15블록 · l10n · GUI 카드 · 픽스처 시험 · 8-1 예제 sb3 | 확장 폴더 · `static/flybrain/*` · `examples_sb3/` | 5~7일 |
| **2 실험실** | 억제 외 실험 블록 · 활동 창 · 가상 더듬이 · 실험 기록 · brain 슬롯 4 · 8-2~8-4 예제 · 벤치마크 | | 5~6일 |
| **3 전뇌 WebGPU(실험)** | compute shader LIF · 모델 호스팅 결정(§12-4) · `gpu-capability` 방식 실측 대결 · 컨텍스트 손실 복구 | 데스크톱 한정 opt-in | 조사 후 산정 |
| **4 로컬 앱** | Python(PyTorch) 전뇌 서버 `ws://127.0.0.1:9300` · §3-2 규격 그대로 · 단일 exe 패키징 · `setEngine` 블록 · Chrome 로컬 네트워크 접근 권한 프롬프트 대응 | 설치 파일·가이드 | 조사 후 산정 |
| **5 온보드** | 표/증류 모델 · ESP32 펌웨어 포함 실험(FW_VERSION 증가 규칙) | | 조사 후 산정 |
| **문서** | 사용자 가이드(랜딩 `10_docs/05_brixelAI_ext_guide/guide_docs/초파리신경망_사용자가이드.md`) · `scratch3_flybrain/CLAUDE.md` · 루트 `CLAUDE.md` 표 갱신 | | 1~2일 |

---

## 10. 표기 규정 (과학적 정직성 — 카드 설명·블록 도움말·가이드 공통)

- 쓴다: "초파리 뇌 **연결지도(FlyWire v783)에 기반한** LIF 신경망 모델(Shiu 외, 2024)의 **부분 회로**" · "감각 입력과 행동 출력의 연결은 **사람이 설계**했다" ·
  "이 모델은 **학습하지 않는다**" · "실제 초파리의 행동을 그대로 재현한다고 주장하지 않는다".
- 안 쓴다: "초파리 뇌를 그대로 옮겼다" · "초파리가 생각한다/학습한다" · "전뇌"(경량 엔진을 가리켜) · "기존 AI 보다 똑똑하다".
- 경량/전뇌/표 모델은 **이름을 분리**한다(`주행 회로(경량)` vs `전뇌(로컬 앱)`). 같은 이름으로 섞지 않는다.
- 비교 실험은 **같은 센서 정보**를 모든 제어기에 준다. 외부 알고리즘이 구한 정답 경로를 신경망이 찾은 것처럼 보이게 하는 장치 금지.
- 출처·인용문을 카드 도움말과 가이드 말미에 싣는다(FlyWire 인용 의무 확인 뒤 문구 확정).

---

## 11. 위험·함정 (미리 적는 것)

| # | 위험 | 대응 |
|---|---|---|
| 1 | 감각→조향 비대칭이 LIF 모델에서 안 나옴 | §9-0 게이트. 우회(중간 주입) 또는 범위 축소. **억지로 되는 것처럼 꾸미지 않는다** |
| 2 | 축소 회로가 전뇌와 다르게 행동 | §6 파이프라인 4번 합격선. 미달이면 N 확대. meta `validation` 에 수치 공개 |
| 3 | JS 와 Brian2 의 수치 차(적분법·지연 반올림·float) | 입력 스파이크 고정 픽스처 시험. 적분법을 원본과 일치 |
| 4 | 워커 번들이 dev 에선 되고 배포본에서 404 | vision-worker 선례대로 2곳 배선 + 빌드 후 `build/` 에 파일 존재 확인. deploy 가드(gui.js 참조 청크 전수 존재)에 걸리는지 확인 |
| 5 | 크롬북에서 step 이 프레임을 넘김 | 워커라 UI 는 안 멈춘다. `계산 속도(배)` 리포터로 보이게. N·dt 조정은 재검증 동반 |
| 6 | 무한반복+복제본이 step 을 폭주 호출 | 엔진 클라이언트 직렬화·Promise 공유(§3-2) |
| 7 | 모델 파일 캐시 불일치 | 파일명 버전 + sha256 |
| 8 | 확장 추가만으로 다운로드·워커 시작 | `loadModel` 에서만(§5-7) |
| 9 | 메뉴가 동적(meta)인데 모델 로드 전 팔레트가 빔 | 기본 모델의 그룹·입력 목록을 **index.js 에 정적 폴백**으로 내장, 로드 후 교체. sb3 에는 영문 키 저장 |
| 10 | 도우미(AI 편집자) 카탈로그 미갱신 → "그런 블록 없어요" | §13 체크리스트(카탈로그 5파일) |
| 11 | 라이선스(상업 이용) 미확인 상태로 배포 | §12-1 결정 전 **운영 배포 금지**. 모델 파일은 우리가 가공한 파생물이라 조건이 그대로 따라온다 |
| 12 | "초파리 뇌로 자율주행" 식 과장 홍보 | §10 표기 규정. 실차·공도 주행 언급 금지, 교육용 저속 로봇 한정 |
| 13 | 자동차 주행 중 브라우저 탭 멈춤·워커 크래시 | 출력이 갱신 안 되면 **마지막 값 유지 금지** → 300ms 무갱신 시 출력 0. 최종 방어는 펌웨어 워치독(기존) |
| 14 | WebGPU 컨텍스트 손실(3단계) | 복구 코드 필수 — 루트 CLAUDE.md 0-U "복구 코드 0건" 리스크 그대로 해당 |
| 15 | 로컬 앱(4단계) — Chrome 의 로컬 네트워크 접근 권한 프롬프트·학교 PC 설치 제한 | 선택 모드로만. 거부 시 자동으로 경량 엔진 폴백 + 상태 표시 |

---

## 12. 사용자 결정 필요

1. **라이선스·인용** — Shiu 저장소 LICENSE 와 FlyWire 데이터 이용 조건 확인 결과에 따라: 상업적 맥락(유료 연수·키트 판매와 결합) 사용 가능 여부, 인용 문구. 불가 조건이면 범위 재논의.
2. 이름: `flybrain` / `초파리 신경망 실험실` — 다른 이름을 원하는지. 카드 위치(`rlmachine` 다음) 동의 여부.
3. 0단계를 **누가 어디서** 돌릴지 — Python+Brian2 환경과 수 GB 메모리가 필요. 사용자 PC(Windows 11)에 conda 설치 동의? GPU 유무?
4. 3단계 전뇌 파일(추정 75~120MB) 호스팅 — GitHub Release / Cloudflare R2 / 하지 않음.
5. 4단계 로컬 앱을 만들지, 만든다면 기존 에이전트(esp32cam·phonecam·ailocal)와 **하나로 합칠지** 별도 exe 로 둘지.
6. 교사 PC 1대가 학급 계산을 맡는 모드(로컬 앱이 에디터를 http 로 서빙)를 범위에 넣을지 — 기본은 **넣지 않음**.
7. 5단계(ESP32 온보드 표 모델)의 우선순위 — "PC 없이 달리는 초파리 자동차"가 교육적으로 필요한지.
8. 작업 정본 폴더 — `G:\내 드라이브\...` 인지 `D:\00_5project_Final\...` 인지.

---

## 13. 착수 체크리스트

- [ ] 루트 `CLAUDE.md` 0번 검증 명령으로 현황 재확인(확장 수·`flybrain` 부재·포트 9300 미사용)
- [ ] §4 표의 "재확인" 항목 전부: `model.py` 파라미터·적분법·포아송 입력 구현·parquet 열 이름·주석 TSV 열 이름·DN 집단의 FlyWire ID
- [ ] §12-1 라이선스 확인 → 결과를 이 문서에 추기
- [ ] 0단계 실행 → `dev-logs/실측_flybrain_스파이크_YYYYMMDD.md` → §9-0 게이트 판정을 사용자에게 보고하고 **승인 후** 1단계
- [ ] VM `scratch3_flybrain/` 4파일 · `extension-manager.js` 등록 · 워커 엔트리(vm webpack) + gui 복사 패턴 · GUI `index.jsx` 카드 · 아이콘 2종(원본 png 보존)
- [ ] 기존 파일 수정 전 `<파일명>_YYYYMMDDHHMM.<ext>` 백업(webpack.config.js · extension-manager.js · index.jsx · l10n json)
- [ ] l10n 최소 ko/en/es/es-419/ja/zh-cn (`flybrain.*`, `gui.extension.flybrain.*`) → `scratch-l10n` `npm run build` → 검증 3종
- [ ] 도우미 카탈로그 5파일: `_tools_agent_catalog` 재생성 3 + `block-aliases.json`·`extension-profiles.json` 수동(별칭: 초파리·뉴런·신경망·커넥톰·뇌)
- [ ] `PROJECT_STOP_ALL` 훅 · 300ms 무갱신 시 출력 0 · 워커 `dispose`(페이지 이탈)
- [ ] `node --check` · 단위시험(`flybrain_lif_core.js`) · vm/gui 빌드 · 8601 dev 서버 **재시작** 후 내장 브라우저 E2E · `brixelPerf` 수치 기록
- [ ] 예제 sb3(`examples_sb3/_tools/README.md` 규칙: 1확장=1파일·스프라이트 1개=예제 1개·그 확장 블록 100% 사용)
- [ ] `scratch3_flybrain/CLAUDE.md` 작성(이름 규칙·파일 구조·함정) · 루트 `CLAUDE.md` 4번 표·0번대 요약 절 갱신 · 이 문서 상태 갱신
- [ ] **커밋·배포는 사용자가 명령할 때만.** 배포는 Git Bash 에서 `bash deploy.sh`

---

## 14. 출처 (2026-09-21 확인)

- Shiu 모델 저장소: https://github.com/philshiu/Drosophila_brain_model (파일 구성·Brian2·자극/억제 방식·"raw output 수 GB")
- fly-survivors: https://github.com/arisson2001rojas-design/fly-survivors (138,639 뉴런·1,500만 시냅스·RTX 4080 SUPER 1.2× 실시간·DN 출력 디코딩·LIF 의 시각 한계)
- Eon fly-brain 벤치마크: https://github.com/eonsystemspbc/fly-brain (Brian2/Brian2CUDA/PyTorch/NEST GPU — 수치는 저장소 CSV 에서 직접 확인 필요)
- Embodied Drosophila: https://github.com/erojasoficial-byte/fly-brain (NeuroMechFly v2 몸체 연결)
- 세포 유형 주석: https://github.com/flyconnectome/flywire_annotations · https://codex.flywire.ai
- 미확인(사용자 제공 요약에만 등장): FlyGM 논문 · DOOMFLY — 인용 전 URL·내용 확인
- 이 문서의 LIF 파라미터·DN 기능 설명 일부는 작성 모델의 기억에 의존 — §4 "재확인" 표시 항목은 원본 대조 전까지 **미검증**으로 취급
